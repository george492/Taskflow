npm install 
```

## Usage

- Frontend : React.js with Recoil for state management, React Router (NavLink) for   navigation, Axios for API communication, Bootstrap for styling and  

responsive layout, and built-in alert messages for user feedback.  
- Backend: Node.js with Express.js. 
- Database: MongoDB (via Mongoose ODM). 
- Authentication: JWT + bcrypt for password hashing